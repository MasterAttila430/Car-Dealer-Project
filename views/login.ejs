<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Belépés</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="auth-container">
        <h1>Belépés</h1>
        
        <% if (error) { %>
            <p class="error-text"><%= error %></p>
        <% } %>

        <form action="/login" method="POST">
            <input type="text" name="name" placeholder="Felhasználónév" required class="form-control">
            <input type="password" name="password" placeholder="Jelszó" required class="form-control">
            <button type="submit" class="btn-block">Belépés</button>
        </form>

        <p>Nincs fiókod? <a href="/register">Regisztráció</a></p>
        <a href="/">Vissza a főoldalra</a>
    </div>
</body>
</html>